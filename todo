- templates for lambdas
- work on overloading functions
- work on multiple generics
- work on function calls for multiple generics
- add a toString method on Omniscript::type and make it easier to get the kindName of a value
- create a separate file for expressions not value.cpp
- add general kind types so that the root/base type is string and not utf32string
- throw an error when the wrong type is inputed in function call
- check if the body needs to be cloned so that generics are properly handled
- handle unresolved types
- handle default arguments / parameters
- handle generics in function bodies
- work on infering types for example a variable decleration with a binary op of two similar types the type of the variable is the type of the two operands
- remove the extra dash underneath generic names with 2 types
- fix IRGenerator::castValue()
- rename all omniscript values to Expressions
- check if references are properly gotten
- add support for 16 bit floats
- add support for dynamic variables
- setting an int var with a char like a returns its ascii '97'
- get the type of a reference from the symbol table before getting its address or creating a variable from it
- handle constants
- fix missing support for big ints 256, 512 and 1024 in value.h
- switching backends
- handle global variables they are assigned locally to top level
- handle nested modules properly
- canvas rendering apis
- make it all statically linked
- fix property and method calls, properties are method calls currently
- get the full absolute path of files (if need be)
- overloading functions by writing more than one argument list like so ??
- function print(int value)
              - (string value)
              - (object value) { 
                
              }
- loading member values from objects likes structs and classes
- time object
- file object
- os object
- ffmpeg converter api
- fix unary minus

- operator overloading
- Runtime and compile time optimizations for big ints
- Global 1024 bit ints
- support f128
- fix pointer depth in resolving llvm types left and right pointer depths
- fix pointer pointee types. I can point to an i32* in a variable that is supposed to point to a char*
- resolve void pointer types correctly
- resolve all string types in statement.cpp
- multidimentional arrays