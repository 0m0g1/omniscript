// Print platform
console.log(OS.getPlatform());

// Sleep for 1000 milliseconds (1 second)
OS.sleep(1000);

// Execute a shell command
OS.execute("echo Hello, OmniScript!");

// Get environment variables
// let envVars = OS.getEnvironmentVariables();
// console.log(envVars);